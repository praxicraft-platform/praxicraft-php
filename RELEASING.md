# Releasing

Maintainer notes for publishing the PHP SDK.

Publish to Packagist as `praxicraft/assess` when ready (not automated in v0.1.0).

## Cut a release

1. Bump the package version.
2. Update `CHANGELOG.md`.
3. Merge to `main` and tag `v{version}` when ready to publish.
